#include <stdio.h>

int main()
{
    int num;
    printf("Please enter num");
    scanf("%d", &num);
    if(num > 0)
    {
     for(int i = 0; i < 20; i++)
     printf("%d %d %d \n", num, i, num * i);
    }
   
   

    return 0;
}
