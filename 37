#include <stdio.h>

int main()
{
int a, b = 1, d;
scanf("%d", &a);
    for(int i = 1; i <= a; ++i)
    {
        d = i;
        while(d)
        {
        printf("%d",b);
        --d;
        }
        printf("\n");
        b++;
    }
    return 0;
}
