//26
#include <stdio.h>

int main()
{
int a, b , c, d = 1;
scanf(" %d %d %d ", &a, &b, &c);
for(int i = 0; i <= a; ++i)
{
    if(a % i ==0 && b % i == 0 && c % i == 0)
    d = i;
}
printf("%d", d);
    return 0;
}
