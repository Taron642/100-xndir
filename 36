#include <stdio.h>

int main()
{
    int num = 100, num_1 = 50, sum = 0;
    for(int i = 0; i < num - num_1; ++i)
    {
        if((num - num_1) % 2 == 0)
        {
            sum += num - num_1;
            num_1 += 1;
        }
    }
    return 0;
}
