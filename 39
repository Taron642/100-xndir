#include <stdio.h>

int main()
{
int a, d;
char b = '*';
scanf("%d", &a);
    for(int i = 1; i <= a; ++i)
    {
        d = i;
        while(d)
        {
        printf("%c",b);
        --d;
        }
        printf("\n");
        
    }
    return 0;
}
